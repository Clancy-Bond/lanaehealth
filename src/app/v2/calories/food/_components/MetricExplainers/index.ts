/**
 * Food-log MetricExplainers barrel.
 *
 * Section-local tap-to-explain modals for the /v2/calories/food
 * surface, mirroring the dashboard pattern in PR #45 + #46.
 */
export { default as MealTimingExplainer } from './MealTimingExplainer'
export type { MealKind } from './MealTimingExplainer'
